// Flattened visibility record used by UI controls.
export interface MappedVisibilityForDepartment {
  id: string
  visible: boolean
  departmentId: string
  departmentName: string
  targetId: string
}
